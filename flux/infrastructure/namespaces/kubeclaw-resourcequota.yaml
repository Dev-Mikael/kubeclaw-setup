apiVersion: v1
kind: ResourceQuota
metadata:
  name: kubeclaw-quota
  namespace: kubeclaw
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    pods: "30"
