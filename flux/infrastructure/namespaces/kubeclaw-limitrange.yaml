apiVersion: v1
kind: LimitRange
metadata:
  name: kubeclaw-default-limits
  namespace: kubeclaw
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
