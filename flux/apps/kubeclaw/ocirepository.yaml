apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: kubeclaw
  namespace: flux-system
spec:
  interval: 30m
  url: oci://ghcr.io/imerica/kubeclaw
  ref:
    semver: ">=0.0.0"
